apiVersion: v1
kind: Service
metadata:
  labels:
    app: synapse
  name: synapse-nodeport
  namespace: matrix
spec:
  type: NodePort
  ports:
    - name: http
      port: 8008
      targetPort: 8008
      protocol: TCP
      nodePort: 30010
  selector:
    app: synapse
